name: Build Blitztext APK

on:
  push:
    branches: [main, master]
    paths:
      - "android-app/**"
      - ".github/workflows/android.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: android-actions/setup-android@v3

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.10.2"

      - name: Ensure stable signing keystore
        working-directory: android-app
        run: |
          if [ ! -f app/blitztext.jks ]; then
            keytool -genkeypair -v -keystore app/blitztext.jks \
              -storepass blitztext -keypass blitztext -alias blitztext \
              -keyalg RSA -keysize 2048 -validity 10000 \
              -dname "CN=Blitztext, O=avf, C=CH"
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add app/blitztext.jks
            git commit -m "CI: add stable signing keystore [skip ci]"
            git push
          fi

      - name: Build signed release APK
        working-directory: android-app
        env:
          VERSION_CODE: ${{ github.run_number }}
          VERSION_NAME: 0.1.${{ github.run_number }}
        run: gradle assembleRelease --no-daemon --stacktrace

      - name: Write version file
        run: echo "${{ github.run_number }}" > android-app/version.txt

      - name: Compatibility copy for existing installs (<=0.1.8 erwarten app-debug.apk)
        run: cp android-app/app/build/outputs/apk/release/app-release.apk android-app/app/build/outputs/apk/release/app-debug.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v7
        with:
          name: blitztext-release-apk
          path: android-app/app/build/outputs/apk/release/app-release.apk

      - name: Publish APK to 'latest' release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: Blitztext (latest)
          files: |
            android-app/app/build/outputs/apk/release/app-release.apk
            android-app/app/build/outputs/apk/release/app-debug.apk
            android-app/version.txt
